---
title: Reactive Map
description: Learn ReactiveMap in solidart
sidebar:
  order: 3
---

A `ReactiveMap` is a reactive wrapper around a `Map` that copies on write.

When you modify any element on the map, the `ReactiveMap` will automatically notify its observers.

```dart
final items = ReactiveMap({'a': 1, 'b': 2});

Effect(() {
  print('Items: ${items.value}');
});

items['c'] = 3; // prints "Items: {a: 1, b: 2, c: 3}"
items.remove('a'); // prints "Items: {b: 2, c: 3}"
```
