name: auth_flow
description: "A new Flutter project."
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.10.0

resolution: workspace

dependencies:
  cupertino_icons: ^1.0.2
  flutter:
    sdk: flutter
  disco: ^1.0.3+1
  flutter_solidart: 3.0.0-dev.2
  go_router: ^17.0.0
  localstorage: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  
